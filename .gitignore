# Dependencies / build artifacts
astro-app/node_modules/
astro-app/.astro/
astro-app/dist/

# Playwright capture artifacts (regenerate via `npm run capture`)
astro-app/_capture/

# Raw browser saves (regenerate by manually re-saving the page)
# Kept locally for re-running scripts/rewrite-saved.mjs, but not committed.
website-save/

# Backup tarballs
*.backup.*.tar.gz
*.tar.gz

# Editor / OS junk
.DS_Store
.vscode/
.idea/

# Env
.env
.env.local
.env.*.local

# Lockfile auto-generated by Nix
flake.lock.bak
